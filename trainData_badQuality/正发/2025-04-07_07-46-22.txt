根据提供的IMU数据，我们可以分析出这个动作确实是正手发球。以下是详细的分析过程和理由：

### 1. **初始阶段（准备动作）**
- 在时间戳 `950464` 到 `950787` 的范围内，加速度 (`accX`, `accY`, `accZ`) 和角速度 (`gyroX`, `gyroY`, `gyroZ`) 的变化相对较小。
- 角度 (`angleX`, `angleY`) 变化平缓，高度 (`height`) 基本保持不变。
- 这表明运动员在准备阶段，手腕和手指的运动幅度较小，主要是调整握拍姿势。

### 2. **发力阶段（挥拍击球）**
- 从时间戳 `950804` 开始，`gyroY` 和 `gyroZ` 的值迅速增大，分别达到约 `150 deg/s` 和 `-20 deg/s`。
- 同时，`accZ` 的值开始显著下降，表明手臂向下挥动。
- 高度 (`height`) 在此阶段略有增加，说明手臂逐渐抬高以准备击球。

### 3. **击球瞬间**
- 在时间戳 `950940` 到 `951025` 的范围内，`gyroX` 和 `gyroY` 的值急剧上升，分别达到约 `-250 deg/s` 和 `180 deg/s`。
- `accX` 和 `accY` 的值也显著增加，特别是在时间戳 `951110` 到 `951144`，`accX` 达到约 `21.94 g`，`accY` 达到约 `8.91 g`。
- 这些特征表明击球瞬间手腕快速下压并向前推送，符合正手发球的动作特点。

### 4. **后续阶段（恢复动作）**
- 击球后，`gyroX`, `gyroY`, `gyroZ` 的值迅速减小，并在时间戳 `951246` 到 `951399` 的范围内逐渐趋于稳定。
- `accX`, `accY`, `accZ` 的值也逐渐减小，表明手臂和手腕的运动逐渐停止。
- 高度 (`height`) 缓慢降低，最终回到初始水平。

### 关键特征总结
- **角速度**：`gyroX` 和 `gyroY` 的峰值较高，尤其是 `gyroX` 的负值较大，表明手腕有明显的下压动作。
- **加速度**：`accX` 和 `accY` 在击球瞬间显著增加，反映了向前推送的力量。
- **角度变化**：`angleX` 和 `angleY` 的变化范围较大，特别是 `angleX` 的负值增加，表明手腕有明显的弯曲和伸展动作。
- **高度变化**：整体高度变化不大，符合发球动作的特点。

### 动作类别确认
综合以上分析，该动作属于 **正手发球**，因为：
1. 击球前有明显的准备动作。
2. 击球时手腕快速下压并向前推送。
3. 击球后动作迅速恢复到初始状态。

如果需要进一步确认，可以结合视频或更多上下文信息进行补充分析。